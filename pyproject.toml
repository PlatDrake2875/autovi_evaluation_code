[project]
name = "autovi-evaluation"
version = "3.0"
description = "Evaluation scripts for the AutoVI dataset"
readme = "README.md"
license = { file = "LICENSE.txt" }
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pillow",
    "tqdm",
    "tabulate",
]
